apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: podinfo
  namespace: podinfo
spec:
  interval: 5m
  values:
    replicaCount: 3
    ui:
      color: "#000000"
      message: "Staging Cluster"
